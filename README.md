# DaySync Workspace

## Firebase quick check

Run this from `frontend` to validate Firebase CLI login, project access, and hosting visibility:

`npm run firebase:check`

For a persistent diagnostic report file:

`npm run firebase:doctor`

This writes `frontend/firebase-check-report.txt`.
